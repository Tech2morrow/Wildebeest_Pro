// Arduino IDE/arduino-cli sketch entry point.
// The implementation is compiled from src/main.cpp; this file intentionally
// contains no duplicate setup() or loop(). PlatformIO remains the primary build.
